//! Test-only crate: see `tests/` and the crate description. There is intentionally no library code; the parity tests
//! live in integration tests so that the Core crates (`weightsim`, `portfolio-construct`) are dev-dependencies only.
