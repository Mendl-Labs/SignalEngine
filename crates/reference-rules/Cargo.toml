[package]
name = "reference-rules"
description = "Pure decision rules of the documented sleeves (ETF trend, crypto trend); no I/O, no network"
edition = "2021"
version = "0.0.1"
publish = false

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["std"] }
sha2 = "0.10"
hex = "0.4"
