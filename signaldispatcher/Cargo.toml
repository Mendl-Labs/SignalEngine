[package]
name = "signal_dispatcher"
version = "0.1.0"
edition = "2021"

[dependencies]
signalengine = { path = ".." }
ultra_signal = { path = "../signal" }
crossbeam = "0.8.4"
tokio = { version = "1.0", features = ["full"] }
