apiVersion: v1
kind: Service
metadata:
  name: {{ include "signal-engine-helm.fullname" . }}
  labels:
    {{- include "signal-engine-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "signal-engine-helm.selectorLabels" . | nindent 4 }}
