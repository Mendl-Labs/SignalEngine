apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "signal-engine-helm.fullname" . }}-test-connection"
  labels:
    {{- include "signal-engine-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "signal-engine-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
